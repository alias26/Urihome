<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mycompany.urihome_mini_web.dao.OrdererDao">
	<select id="getOrderItem" parameterType="String" resultType="map">
	SELECT c.cnumber, c.mid, p.pid, p.pname, p.pprice, p.pstock, p.pdate, p.psellamount, p.psales, p.producthicount, c.pbuyamount
	FROM cart c
	INNER JOIN product p ON c.pid = p.pid;
	</select>
</mapper>